// Definition d'une classe
class Livre {
  constructor(titre, author) {
    this.titre = titre;
    this.author = author;
  }
}
// Nouvelle instance de classe
let newBook = new Livre("La tour sombre", "Moimm");

console.log(newBook);

// SECOND

let secondBook = new Livre("Dark tower", "Moimm");
console.log(secondBook);
